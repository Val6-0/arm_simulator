#ifndef __SYNTAX__
#define __SYNTAX__

#include "lexical_analysis.h"

void syntax_verification(linked_L *list);
// Applies the syntax verification to the linked list,
// using the syntax defined in the readme.md
// if an error is detected, display an error message and exit with code 0

#endif